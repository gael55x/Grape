{
  "name": "grape",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first incremental context compiler for AI coding agents.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "docs:check": "node scripts/check-docs-structure.mjs",
    "contracts:check": "node scripts/check-shared-contracts.mjs",
    "state:check": "node scripts/check-state-machine.mjs",
    "snapshot:check": "node scripts/check-alpha-snapshot.mjs",
    "fixtures:check": "node scripts/check-fixtures.mjs",
    "check": "npm run docs:check && npm run contracts:check && npm run state:check && npm run fixtures:check && npm run snapshot:check"
  }
}
