# Monorepo Lite Fixture

This fixture proves that task-specific retrieval can keep context focused on a
nested package when package path and symbols are explicit.

It does not claim full package-boundary invalidation or per-package retrieval caps.
