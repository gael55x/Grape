# Grape Fixture Rules

- Always include exact code spans for discount calculation behavior.
- Treat test assertions as evidence only when tied to a matching source hash.
- Resend pinned project rules when producing a context pack.
