# Clean TypeScript App Fixture

This fixture is the first Alpha Context Loop repository.

It exists to prove the smallest safe Grape loop:

```text
repo snapshot -> evidence -> proof-backed claim -> current-valid context -> artifact -> diff
```

The fixture is intentionally small. It is not a runnable product app and should not grow into a framework sample.
