# Polyglot Fallback Fixture

This fixture proves that non-TS languages still produce safe path and lexical context.

It intentionally contains Python, Java, and Kotlin files without requiring Grape to
claim full semantic graph support for those languages.
