{
  "_example": {
    "scenario": "artifact-level secret scan blocks compile output",
    "fixture": "synthetic",
    "expectedState": "CLI exits with code 2; no artifact JSON is written",
    "expectedWarnings": [],
    "relatedTests": [
      "artifact secret scan blocks raw secret-looking assignments"
    ]
  },
  "exitCode": 2,
  "errorCategory": "unsafe_compile",
  "message": "artifact secret scan blocked context artifact JSON: env_secret_assignment",
  "note": "Illustrative failure shape only. Output never includes raw secret values."
}
