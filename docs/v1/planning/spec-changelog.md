# V1 Spec Changelog

This file records changes to the V1 implementation contract.

Each entry should include:

- date
- changed section
- reason
- affected docs
- affected tests
- migration or benchmark impact, if any

## Unreleased

- 2026-06-19: Added Codex-native MCP setup and server instructions. `grape mcp --install --client codex` writes or merges project-local `.codex/config.toml` by adding or replacing only `[mcp_servers.grape]`; it preserves unrelated TOML, refuses malformed table headers, treats identical Grape entries as already configured, requires `--force` for conflicting Grape entries, supports `--dry-run`, and prints a `codex mcp add grape -- ...` fallback command. MCP `initialize` now returns server instructions for session identity, invalidation, and omitted restore behavior. Affected docs: README, getting started, agent sessions, CLI, MCP tools, SPEC, implementation status, changelog files. Tests added: Codex MCP client install behavior tests and MCP initialize instruction coverage. Migration impact: none. Benchmark impact: none.
- 2026-06-19: Added `grape mcp --print-agents-snippet` as a read-only setup helper. It prints path-neutral AGENTS.md guidance for Grape MCP usage and does not edit repository rule files. Affected docs: README, getting started, agent sessions, CLI, MCP tools, SPEC, changelog files. Tests added: CLI snippet output and action exclusivity tests. Migration impact: none. Benchmark impact: none.
- 2026-06-19: Added a repo-local Codex plugin at `plugins/grape` with `grape mcp --stdio` config, a Grape skill, and `.agents/plugins/marketplace.json`. The plugin does not include hooks and assumes `grape` is on `PATH`; direct Codex config install remains the path for exact repository working-directory pinning. Affected docs: README, getting started, agent sessions, CLI, MCP tools, SPEC, changelog files. Tests added: Codex plugin structure and package inclusion checks. Migration impact: none. Benchmark impact: none.
- 2026-06-19: Added `npm run codex:check` as the local Codex workflow gate. It verifies Codex config installation in a temp repo, AGENTS snippet output, MCP initialize/context/restore over stdio, plugin config shape, and isolated Codex CLI marketplace/plugin installation when the Codex CLI is available. Affected docs: README, getting started, agent sessions, CLI, MCP tools, implementation status, changelog files. Tests added: `scripts/check-codex-local-workflow.mjs`. Migration impact: none. Benchmark impact: none.
- 2026-06-19: Compacted compression artifact dependency scope in context artifacts. Compression cache storage still keeps detailed input refs and hashes in `compression_inputs`, but artifact dependency manifests now store aggregate `inputHash`, `inputCount`, policy/scope hashes, and output hash instead of copying full `inputRefs` and `inputHashes` arrays into public artifact scope. Affected docs: SPEC, compression, context artifact, storage, implementation status, changelog files. Tests added: large compression dependency scope regression test. Migration impact: none. Benchmark impact: storage and payload measurements must use this compact scope as the baseline.
- 2026-06-19: Added local retention defaults for context artifacts, snapshots, FTS rows, compression inputs, derived metadata, and invalidated records. New configs write the block; older schema-1 configs without it read with defaults. Affected docs: SPEC, getting started, CLI, storage, security, changelog files. Tests added: config default write and old-config compatibility tests. Migration impact: no SQLite migration; local config remains schema 1. Benchmark impact: none.
- 2026-06-19: Added `grape compact` as a preview-first local maintenance command for context artifact retention. Without `--confirm`, it deletes nothing. With `--confirm`, it deletes only eligible old context artifact rows, cascaded artifact-owned rows, and matching regular files under `.grape/artifacts/`, while preserving latest-per-session artifacts, active sent context, restorable omitted context, and locked sessions. Affected docs: SPEC, getting started, CLI, storage, security, changelog files. Tests added: compact CLI preview/apply/protection behavior. Migration impact: none. Benchmark impact: none.
- 2026-06-20: Extended `grape compact` to apply compression cache retention. Without `--confirm`, it reports eligible unreferenced compression cache rows. With `--confirm`, it deletes eligible `compression_artifacts` rows and their cascaded `compression_inputs`, while preserving compression artifacts still referenced by surviving context artifacts. Affected docs: SPEC, getting started, CLI, storage, compression, security, changelog files. Tests added: compact CLI compression preview/apply/protection behavior and maintenance repository compression retention tests. Migration impact: none. Benchmark impact: storage measurements should include compression cache before and after compaction.
- 2026-06-20: Extended `grape compact` to apply FTS row retention. Without `--confirm`, it reports eligible old searchable text rows. With `--confirm`, it deletes only whole snapshot groups from `fts_entries`; SQLite cascades matching `fts_entry_text` rows. It preserves FTS rows for the latest repo snapshot and does not delete source records, source files, repo snapshots, claims, proofs, source rejections, audit rows, or the database file. Affected docs: SPEC, getting started, CLI, storage, security, changelog files. Tests added: compact CLI FTS preview/apply behavior, FTS-only confirmation behavior, and maintenance repository FTS retention tests. Migration impact: none. Benchmark impact: storage measurements should include FTS rows before and after compaction.
- 2026-06-20: Extended `grape compact` to apply derived metadata retention. Without `--confirm`, it reports eligible old `symbol_nodes` and `symbol_edges` rows. With `--confirm`, it deletes only whole snapshot groups from those tables. It preserves symbol metadata for the latest repo snapshot, symbol rows still referenced by surviving context artifact dependencies, and candidate snapshots still referenced by symbol edges from another snapshot. It does not delete source records, source files, repo snapshots, claims, proofs, source rejections, audit rows, or the database file. Affected docs: SPEC, getting started, CLI, storage, security, changelog files. Tests added: compact CLI derived metadata preview/apply behavior, derived-only confirmation behavior, and maintenance repository derived metadata retention tests. Migration impact: none. Benchmark impact: storage measurements should include symbol metadata rows before and after compaction.
- 2026-06-20: Extended `grape compact` to apply orphan snapshot retention. Without `--confirm`, it reports eligible old snapshot rows. With `--confirm`, it deletes only `repo_snapshots` rows that are not latest for their repo and have no sessions, artifacts, compression rows, FTS rows, symbol rows, source rows, or context dependencies; SQLite deletes their matching `worktree_states`. It rechecks snapshot eligibility inside the apply transaction after artifact, compression, FTS, and symbol cleanup. It does not delete source records, source files, claims, proofs, project rules, source rejections, audit rows, or the database file. Affected docs: SPEC, getting started, CLI, storage, security, changelog files. Tests added: compact CLI snapshot-only and transaction recheck behavior, plus maintenance repository snapshot protection and deletion tests. Migration impact: none. Benchmark impact: storage measurements should include orphan snapshots and worktree rows before and after compaction.
- 2026-06-20: Extended `grape compact` to apply invalidated-record retention. Without `--confirm`, it reports old closed invalidation pairs. With `--confirm`, it deletes an `INVALIDATE_PREVIOUS` pack row only when it also deletes the stale sent row it points at and that stale row's original pack row in the same transaction. It protects marker-only cases and artifacts that own needed invalidation markers so stale context cannot become active again. Affected docs: SPEC, getting started, CLI, storage, security, context diff, changelog files. Tests added: invalidated-record preview, confirmation, deletion, marker-only refusal, stale-context safety, and artifact marker protection tests. Migration impact: none. Benchmark impact: storage measurements should include invalidated sent and pack rows before and after compaction.
- 2026-06-17: Added MCP client config auto-wiring for Cursor and Claude Desktop. `grape mcp --install --client cursor` writes or merges `.cursor/mcp.json`; `grape mcp --install --client claude` writes or merges Claude Desktop `claude_desktop_config.json` only when the platform path can be resolved safely. The install flow preserves unrelated config, fails safely on invalid JSON, treats identical Grape entries as already configured, requires `--force` for conflicting existing `mcpServers.grape` entries, supports `--dry-run`, and keeps `grape mcp --print-config` as the manual fallback for other clients. This is separate from the 1.0.0-beta.7 stdio framing fix, which did not write client config. Affected docs: README, getting started, agent sessions, CLI, MCP tools, SPEC, implementation status, changelog files. Tests added: Cursor and Claude MCP client install behavior tests plus installed-package dry-run checks in package smoke and beta client trial. Migration impact: none. Benchmark impact: none.
- 2026-06-17: Changed MCP stdio transport from `Content-Length` header framing to newline-delimited JSON-RPC messages to match the MCP stdio transport contract used by real clients. `grape mcp --stdio` now writes one compact JSON-RPC object per line, accepts LF or CRLF input lines, rejects `Content-Length` headers with a clear parse error, and keeps the 4 MiB message-line guard. Affected docs: MCP tools, agent sessions, testing, beta readiness, implementation status, changelog files. Tests updated: MCP behavior tests, contract tests, install/e2e smoke helpers, and packaged beta client trial. The packaged beta client trial now covers installed CLI core workflows plus MCP session workflows. CI impact: the packaged beta client trial now runs on Ubuntu, macOS, and Windows. Migration impact: none. Benchmark impact: none.
- 2026-06-13: Added `npm run beta:client-trial` as the automated packaged-install MCP client trial gate. It exercises `initialize`, `tools/list`, `grape_get_status`, two-turn `grape_get_context`, omission, restore, source invalidation, branch invalidation, reset, mismatch recovery guidance, status redaction, and ignored secret-looking file rejection over stdio from a packed install. `npm run beta:check` now runs `check`, `benchmark:run`, `e2e:alpha`, and `beta:client-trial`. GitHub Actions `beta-smoke` runs `benchmark:run`, `e2e:alpha`, and `beta:client-trial` after the cross-platform `check` matrix. Affected docs: README, testing, benchmarks, MCP tools, CLI, beta readiness, beta trial checklist, roadmap, implementation status, changelog files. Tests: scripted trial in `scripts/run-beta-client-trial.mjs`. Migration impact: none. Benchmark impact: none; trial is a transport gate, not an external comparison claim.
- 2026-06-13: Moved `grape bench` default tasks into each fixture's `grape-fixture.json` `benchmarkTask` field. `--task` remains an explicit override. The scripted benchmark suite now includes `polyglot-fallback-repo` and `monorepo-lite-repo` as internal no-change transport fixtures. Affected docs: benchmark, CLI, fixtures, language indexing, beta readiness. Tests added: benchmark CLI default-task coverage and fixture metadata checks. Benchmark impact: fixture estimates now cover the polyglot fallback and monorepo-lite scenarios without making external comparison claims.
- 2026-06-13: Omitted experimental `agentGraph` from default MCP `agent_pack` output. The compact response keeps structured `contextPackItems`, `artifactRef`, diff summary, warnings, budget, and artifact file refs; `outputMode: "full"` remains the graph inspection path. Affected docs: SPEC, context diff, context artifact, transport stability, MCP tools, CLI, examples, benchmarks, changelog, beta readiness, spec changelog. Tests added: default agent_pack lacks `agentGraph` and full inspection output still returns the graph cut. Benchmark impact: default serialized agent-output estimates no longer include graph adjacency.
- 2026-06-13: Omitted optional inline `contextPackMarkdown` from default MCP `agent_pack` output. The compact response keeps structured `contextPackItems`, `artifactRef`, diff summary, warnings, budget, and artifact file refs; `outputMode: "full"` and stored artifact Markdown remain the Markdown inspection paths. Affected docs: SPEC, context diff, context artifact, transport stability, MCP tools, CLI, benchmarks, changelog, beta readiness, spec changelog. Tests added: default agent_pack lacks inline Markdown and full inspection output still renders Markdown. Benchmark impact: default serialized agent-output estimates no longer include duplicated Markdown.
- 2026-06-13: Added indexed package provider summaries to the `index-blind-spots` artifact section. Grape now reports package-root, language, provider, file count, capabilities, and provider capability gaps for indexed module nodes with package-root metadata, including packages outside the selected task context. Affected docs: language indexing, context artifact contract, beta readiness, implementation status, spec changelog. Tests added: unselected package provider summary output in monorepo and nested-package fixtures. Migration impact: none.
- 2026-06-13: Made path-like test seed caps a real reservation when explicit source seeds also fill the source cap. Grape now limits explicit seed fill enough to include capped test seed refs when the cap can fit both roles, while one-slot caps keep explicit source priority. Affected docs: retrieval, beta readiness, spec changelog. Tests added: mixed explicit/test seed cap selection and one-slot source priority. Migration impact: none.
- 2026-06-12: Restricted import graph edges to AST-backed files. Generic fallback files still produce file nodes, lexical rows, and conservative declaration anchors, but Grape no longer emits JS-style regex import edges from providers that declare a `module_edges` capability gap. Affected docs: language indexing, beta readiness, changelog, spec changelog. Tests added: generic fallback import-looking text does not create import edges. Migration impact: none.
- 2026-06-12: Spread capped Tier 2 retrieval across exact evidence roles. After existing package and language spreading, task retrieval now alternates direct symbol-match refs and related-test refs within Tier 2 so one exact evidence role cannot spend every small source cap before the other appears in the same group. Affected docs: retrieval, beta readiness, spec changelog. Tests added: capped Tier 2 source and related-test evidence selection. Migration impact: none.
- 2026-06-12: Added compact retrieval warning codes for capped package and language coverage. Task retrieval now emits `task_retrieval_package_groups_omitted_over_cap:<count>` when a source cap leaves package-root groups unrepresented, and `task_retrieval_language_groups_omitted_over_cap:<count>` when it leaves known source-language groups unrepresented. Compile recovery guidance tells agents to narrow or seed package/language scope instead of changing token budget. Affected docs: retrieval, transport stability, CLI, MCP tools, beta readiness, spec changelog. Tests added: omitted package/language group warnings, stable warning pattern checks, compile recovery guidance. Migration impact: none.
- 2026-06-11: Added the beta transport/schema stability boundary. `docs/v1/contracts/transport-stability.md` now owns stable, experimental, inspection-only, and debug-only MCP transport classifications plus the warning taxonomy. SPEC keeps a type summary and points to the stability doc instead of duplicating the field matrix. Affected docs: SPEC, MCP tools, contract README, beta readiness, examples. Tests added: focused beta transport shape contract tests. Migration impact: none; no standalone output JSON Schema artifact required for controlled beta.
- 2026-06-11: Bounded missing seed warnings in task source retrieval. Missing file/test seed refs emit at most five per kind, followed by `task_seed_file_not_found_omitted:<count>` or `task_seed_test_not_found_omitted:<count>` for additional missing seeds. Reserved test-seed caps now emit truncation warnings even when total candidates do not exceed the global source cap. Affected docs: retrieval, transport stability, spec changelog. Tests added: bounded missing seed warnings and reserved test-seed omission warnings. Migration impact: warning output is more compact.
- 2026-06-11: Allowed task-selected rule-file excerpts to satisfy high-risk exact-context policy when the excerpt body contains overlay-local evidence. This keeps high-risk matching excerpt-local and fail-closed while aligning source/config/rule behavior with the transport warning taxonomy. Affected docs: transport stability, spec changelog. Tests added: task-selected rule-file auth evidence satisfies the auth overlay. Migration impact: none.
- 2026-06-11: Defined `rankedSourceRefs` beta contract. `rankedSourceRefs` now contains the final capped source refs in deterministic retrieval-priority order, with the same membership and order as `selectedSourceRefs`. It is not a pre-cap semantic reordering and not pure global semantic-score order. Equal-score ties within a tier use stable byte-string comparison rather than discovery insertion order. The field name is retained to avoid transport churn. Affected docs: retrieval, spec changelog. Migration impact: none.
- 2026-06-11: Hardened high-risk excerpt body matching with shared tokenizer and expanded overlay terms. `excerptBodyMatchesRiskTerms` now uses a shared `tokenizeRiskEvidenceText` function that splits camelCase, lowercases, and tokenizes both body text and risk terms before matching, preventing short-term substring false positives such as `auth` matching `author`. Risk overlay term sets are expanded with explicit safe tokens aligned with task-detection patterns. Affected docs: transport stability, spec changelog. Tests added: `author`-only body plus auth overlay stays unsafe; `requireSession` camelCase body satisfies auth overlay. Migration impact: none.
- 2026-06-11: Removed `task_retrieval_omitted_over_cap_sample` from default retrieval output. Compact agent-facing output now emits only `task_retrieval_truncated` and `task_retrieval_omitted_over_cap:<count>`. The sample ref list is no longer part of the stable warning taxonomy. Affected docs: retrieval, spec changelog. Migration impact: none.
- 2026-06-11: Fixed tier-aware rank-before-cap task source retrieval. Candidates partition into explicit source, bounded test/failure seeds, exact symbol/test-relationship, and expansion tiers; Grape ranks within each tier, selects in tier-priority order, and emits compact truncated/omitted warnings. Explicit refs get priority, not immunity beyond the cap. Affected docs: retrieval, spec changelog. Tests required: tier retention, explicit over-cap ranking, bounded test seeds, compact omitted warnings, output-shape invariants. Migration impact: none. Not a benchmark claim; beta readiness is not claimed from this fix alone.
- 2026-06-11: Tightened high-risk exact-span policy to require excerpt-local body evidence. Path/filename matches no longer satisfy `risk_overlay` checks; zero body-local matches fail closed with `risk_overlay_missing_exact_context`. Affected docs: context artifact contract, spec changelog. Tests required: path-only rejection and existing auth body pass/fail cases. Migration impact: none. Not a benchmark claim.
- 2026-06-08: Added advisory `semantic_candidate` retrieval ranking. Local deterministic scoring over task terms, symbols, path segments, and lexical matches reorders `rankedSourceRefs` for compiler preference ordering only; impact selection membership remains in `selectedSourceRefs`. Semantic candidates are not proofs, not durable claims, and are rejected when used as `proofSignalKind = semantic_candidate`. Affected docs: retrieval, trust model, testing, spec changelog. Tests required: semantic candidate generation, ranked-ref ordering only, policy rejection, advisory wording, artifact section labels, durable-claim exclusion. Migration impact: none.
- 2026-06-08: Added global trust wording guardrails for generated claim text, artifact sections, CLI/MCP inspection output, compression orientation, status messages, and benchmark reports. Grape rejects forbidden root-cause, fix-proof, semantic-authority, benchmark-savings, and agent-enforcement phrases in MCP candidate claim text and uses conservative disclaimers in durable claim generators. Active claim artifact sections render as `Scoped Proof-Backed Claims (Current-Valid)` with a scope footer. Affected docs: trust model, context artifact contract, retrieval, CLI, MCP, testing, changelog. Tests required: forbidden phrase detection, durable claim generator wording, artifact section title/footer, MCP candidate rejection. Migration impact: none.
- 2026-06-08: Enabled the narrow `observed_test_failure_span_link` durable claim policy for failed Grape-observed test runs. Claims require direct `observed_test_failure_relation` proof from a trusted failed `test_run` source, hash-only failure-output metadata, bounded exact span proof refs, separate evidence slots, and conservative missing-evidence warnings. They prove only that Grape observed a failed test run and linked available evidence to candidate test/source spans. They reject code-wrong, caused-failure, root-cause, and fix-validity interpretations. Affected docs: trust model, storage, retrieval, context artifact contract, testing, changelog. Tests required: failure-location extraction privacy, policy overclaim rejection, persistence with hash-only output, CLI artifact rendering without raw logs, and preservation of manifest dependency and observed-run behavior. Migration impact: none.
- 2026-06-07: Enabled the narrow `package_manifest_dependency_exists` durable claim policy for trusted npm `package.json` dependency entries. Claims require direct `package_manifest_dependency_entry` proof, current source hash, manifest/package scope, provider provenance, line span, and dependency specifier hash; they prove only the wording `Manifest declares dependency <dependency-name>.` and reject installed-package, usage, runtime, safety, validity, lockfile, and correctness interpretations. Affected docs: SPEC, trust model, storage, language indexing, retrieval, context artifact contract, testing, changelog. Tests required: manifest dependency policy rejection, provider-backed proof persistence, current manifest hash/package-scope filtering, conservative conflict blocking, and CLI artifact privacy without raw manifest specifiers. Migration impact: none.
- 2026-06-06: Renamed the internal omitted-restore artifact backing file to `.repository.json` and removed the old backing-file restore lookup. Public CLI/MCP artifact inspection still returns only public V1 artifact JSON or metadata and must not expose backing-file bodies. Affected docs: SPEC, context artifact contract, context diff contract, CLI, MCP, storage, security, trust model, testing/status. Tests required: CLI restore tamper tests, public artifact contract tests, MCP artifact/restore tests. Migration impact: none.
- 2026-06-06: Scoped observed test-run rendering to explicit test file refs. `grape test` records safe repo-relative test file arguments as hash-backed `testFiles` metadata, and selected-source artifacts render current-session observed test-run claims only when those refs match selected task/test refs. This metadata proves only that the run named the file, not coverage, correctness, or root cause. Affected docs: SPEC, trust model, storage, retrieval, CLI, context artifact contract, testing. Tests required: explicit test file metadata and unrelated observed-run omission from task artifacts. Migration impact: none.
- 2026-06-06: Enabled the narrow `repository_symbol_declaration_exists` durable claim policy. Provider-backed symbol declaration claims require a trusted allowed repository source, high-confidence AST symbol metadata, direct `provider_symbol_declaration` proof support, current source hash, and declaration body hash. The claim proves declaration-span existence only and rejects imports, call graph completeness, behavior, correctness, root cause, ownership, and architecture conclusions. Affected docs: SPEC, trust model, storage, testing. Tests required: symbol claim policy and provider proof persistence without raw body. Migration impact: none.
- 2026-06-06: Added related-test relationship evidence to task retrieval artifacts. The task-retrieval section may render which supported import/call edge selected a related test file, but this is selection evidence only and cannot be used as proof that a test ran, covered behavior, found root cause, or established correctness. Affected docs: SPEC, context artifact contract, retrieval, testing. Tests required: focused task retrieval relationship output and monorepo artifact rendering. Migration impact: none.
- 2026-06-06: Added dependency-backed relationship refs for related-test retrieval evidence. When an import/call relationship comes from the indexed symbol graph, task retrieval may render the stable relationship ref and include the matching symbol-edge dependency while keeping `proofRefs` empty. Affected docs: SPEC, context artifact contract, retrieval, testing. Tests required: relationship ref carry-through and dependency-backed artifact rendering without proof refs. Migration impact: none.
- 2026-06-06: Added explicit claim-edge authority metadata. Claim edges now have sidecar provenance covering creator authority, confidence, reason, and hash-only metadata; review-only authorities cannot block current-valid claims; legacy conflict edges fail closed with warnings only where conflict uncertainty would otherwise become active truth. Affected docs: SPEC, trust model, storage, testing. Tests required: edge authority policy, conflict resolution authority, storage migration from previous schema, and public conflict inspection tests. Migration impact: add `claim_edge_authority`.
- 2026-06-05: Clarified the durable claim policy boundary for the currently enabled claim types. `repository_source_excerpt_exists`, `project_rule`, and `grape_observed_run_result` now have explicit accepted proof/source/support requirements and forbidden interpretations; broader V1.0 claim families remain disabled until policy entries, fixtures, and current-valid tests exist. Affected docs: SPEC, trust model, retrieval, testing. Tests required: claim policy rejection and overclaim safety tests. Migration impact: none.
- Clarified language-agnostic indexing: Grape transport must safely handle unsupported languages through exact/path/lexical fallback and explicit blind spots, while language-aware graph extraction is provider/capability-based and currently strongest for TypeScript/JavaScript.
- Clarified monorepo retrieval expectations: package/workspace boundaries should be detected and preserved for source budgets and dependency refs where possible, with partial warnings when boundaries are unknown.
- Aligned beta trial and implementation-order requirements with safe polyglot fallback: Python, Java, Kotlin, Go, Rust, and other allowed text files must be safe fallback inputs, while stronger graph extraction requires providers, fixtures, and capability diagnostics.
- Clarified the local Grape-observed command/test runner contract: `grape run` and `grape test` execute from the repository root against an existing current context session, create observed run IDs, persist trusted redacted `command_run` / `test_run` source evidence with command/output hashes, reject secret-looking commands before execution, and promote only the narrow `grape_observed_run_result` proof/claim.
- Clarified the current V1 retrieval contract: lightweight source selection uses explicit seeds, safe lexical rows, lightweight symbols/imports, and current-valid narrow source-excerpt claims; it reports truncation/no-match warnings and remains an impact candidate selector rather than full graph or semantic retrieval.
- Added the Beta Retrieval V2 contract: deterministic TypeScript/JavaScript AST graph expansion may select source, test, import, export, and call-related context, while exact excerpts, rules, proofs, dependency manifests, and current-valid filtering remain authoritative.
- Clarified observed-run retrieval: current-valid `grape_observed_run_result` claims from the current task-bound session may render in context, but they prove only the observed command/test result and never broader correctness.
- Added parsed durable rule promotion: safe verified rule-file lines may become current-valid `project_rule` claims backed by direct `exact_project_rule_excerpt` proofs, while exact rule excerpts remain pinned and rule digests remain orientation only.
- Added conservative conflict workflow for parsed project rules: deterministic opposing-topic rule pairs may create `needs_review` claim edges, and local CLI resolution records non-conflict resolution edges without refuting or promoting claims. MCP conflict tooling remains read-only.
- Clarified current-valid edge filtering: explicit active `contradicts`, `violates`, and `supersedes` claim edges can block otherwise eligible claims, while `needs_review` remains inspection metadata unless an explicit blocking edge exists.
- Clarified lazy artifact inspection: compact MCP context output carries `artifactRef.fullArtifactTool`; `grape_get_artifact` defaults to metadata and returns stored public artifact JSON only when called with `outputMode: "full"`.
- Clarified repo-scoped public output sanitization: CLI/MCP/benchmark outputs sanitize against the active repo root and common local path aliases by default, and debug/fixture-miss paths must not expose raw local filesystem structure.
- Clarified local-project observation ownership: public observation APIs stay exported from `src/app/local-project/observations.ts`, while validation, path normalization, source construction, and persistence orchestration live in focused files under `src/app/local-project/observation/`.
- Clarified local-project workflow ownership: setup, context compilation, inspection, omission restore, restricted writes, observation, source excerpts, and public contract types live in named subdirectories under `src/app/local-project/`, with root files limited to public export surfaces.
- Aligned the published alpha runtime floor with package enforcement: `grape-context@0.1.0-alpha.2` requires Node.js 22.13+.
- Refocused V1 product framing on the Context Transport Protocol (`ContextPackItem` diff as the outward contract). Added ADR-0010, updated root `ROADMAP.md`, `implementation-roadmap.md`, `docs/v1/README.md`, and SPEC §0 executive summary. No schema or behavior change in this docs-only slice.
- Established public V1 documentation architecture derived from the canonical V1 contract.
- Published the canonical V1 implementation contract as `docs/v1/SPEC.md` so future implementation does not depend on ignored planning files.
- Clarified implementation planning around goal names: Documentation Foundation, In-Memory Context Loop, and Alpha Product Slice.
- Hardened supporting domain docs with schemas, transition gates, storage rules, MCP contracts, compression invalidation, tests, benchmark rules, and security gates derived from `SPEC.md`.
- Organized supporting docs into purpose-based folders while keeping `docs/v1/SPEC.md` as the canonical top-level contract.
- Added In-Memory Context Loop plan to constrain the first implementation loop.
- Aligned supporting storage table names with canonical `SPEC.md` names where present.
- Added the initial storage migration contract for project, repo, source, claim, proof, artifact, and session-ledger tables.
- Hardened the initial storage contract with SQL checksum validation, serialized-state constraints, session branch/worktree identity, dependency manifest hashes, and complete sent/omitted ledger fields.
- Selected Node 22.5+ built-in `node:sqlite` for the initial runtime migration path to avoid native SQLite package installation.
- Made Node 22.5+ the canonical V1 runtime and added ADR-0004 for the no-native SQLite runtime decision.
- Tightened the storage contract so sent, omitted, and pack ledger rows must reference artifacts owned by the same session.
- Tightened restorable omission storage so restore metadata, branch/commit identity, dependency manifest hash, send count, and token count are persisted.
- Added runtime migration bootstrap protection for non-empty databases without trusted migration metadata.
- Added Durable Context Build Proof as the next implementation goal before CLI/MCP product transport.
- Added code modularity standards with split checkpoints to prevent godfiles and generic utility modules.
- Extended storage dependency kinds for persisted artifact manifests that include repo snapshot, worktree state, and session ledger dependencies.
- Extended the repo snapshot implementation contract with dirty paths, deterministic snapshot hashes, Git-visible file hashing, ignored-file exclusion, and symlink hashing without following symlink targets.
- Clarified the implementation contract for snapshot-derived evidence storage: allowed files become source records, rejected ignored/private/unreadable paths become source-rejection records, and rejected file contents remain unread.
- Added an explicit implementation need for `symbol_nodes` and `symbol_edges` through `0002_indexing_foundation.sql` so V1 has persisted file relationship tracking before repository-derived artifact compilation.
- Clarified the current repository-derived compiler foundation as an internal repository artifact compiler that consumes persisted snapshot/source/index records while emitting the public V1 `ContextArtifact` contract.
- Clarified the initial CLI compile output path. Later slices changed public JSON to a V1 `ContextArtifact` while keeping internal repository backing files for restore verification.
- Clarified the first MCP stdio implementation status: `grape_get_context` and `grape_get_status` are implemented against the local compile/status services while the full V1 MCP tool surface remains pending. The current MCP context tool requires `sessionId` or `agentSessionId` and returns repo-relative artifact file refs. Later slices added seed-aware source retrieval and token-budget evaluation.
- Clarified the omitted restore contract: CLI `grape omitted` and MCP `grape_get_omitted_item` restore only session-scoped omitted sections after validating token ownership, stored artifact identity, artifact hash, stored dependency rows, section hash, dependency manifest, redaction status, branch, commit, worktree hash, and source/config/lockfile/rule dependency hashes.
- Clarified artifact inspection: CLI `grape artifacts` and MCP `grape_get_artifact` expose metadata and dependency rows for inspectability without returning internal repository backing files.
- Clarified branch-switch session behavior for the product slice: explicit session reuse across Git branches updates the session compile state under lock, records a `branch_changed` session invalidation event, and invalidates stale sent context instead of treating previous-branch context as unchanged.
- Clarified exact-source evidence: repository-derived artifacts may include bounded exact excerpts from selected allowed sources with deterministic proof refs and proof dependency hashes, but these refs do not promote durable claims or satisfy high-risk task policies by themselves.
- Clarified session reset recovery for the product slice: explicit reset requests invalidate active prior sent context and force current sections to be resent instead of relying on omitted unchanged context.
- Clarified active rule handling for the product slice: trusted rule files can be rendered as pinned exact-context sections before parsed durable project-rule records exist.
- Clarified lexical storage foundation behavior: lexical search rows may be persisted for allowed source records only after source-hash verification and secret-looking text rejection.
- Clarified the internal-to-public boundary: internal diff rows can remain an implementation detail when public CLI/MCP/artifact JSON output is mapped to V1-shaped `ContextPackItem` objects.
- Clarified task source retrieval behavior for the product slice: safe lexical rows, symbol/path metadata, and MCP seed refs may prioritize source evidence selection, but they do not replace durable current-valid claim retrieval or high-risk exact-span policy.
- Clarified token-budget behavior for the product slice: requested budgets are evaluated without pruning required context, and budgets below pinned/exact/invalidation context fail closed with `token_budget_below_required_context`.
- Clarified the public artifact schema bridge: compile outputs now expose a V1 `ContextArtifact` in public JSON and MCP output, while internal repository artifact bodies are stored in `.repository.json` backing files for restore verification only.
- Clarified current-scope plumbing: CLI/MCP compile output, public artifacts, dependency refs, retrieval filters, and claim persistence use one normalized current-scope collector with redacted feature-flag count/hash and explicit warnings.
- Clarified schema gaps in `ContextInput`, `ContextDependency`, and `ContextArtifact`: repo snapshot, worktree state, and session ledger dependencies are valid invalidation inputs, and `tokenBudget` is optional when no budget was requested.
- Clarified proof inspection during the pre-claim proof work: `grape proofs`, `grape proofs --proof <proof_id>`, and `grape proofs --source <source_id>` are valid inspection commands alongside the future claim-linked `grape proofs <claim_id>` form.
- Clarified the first high-risk exact-context policy: risk overlays are safe only when task retrieval or explicit seed refs select proof-backed exact source/config/rule evidence; otherwise compile fails closed with `risk_overlay_missing_exact_context`.
- Clarified the first durable claim type: `repository_source_excerpt_exists` may be promoted from a validated direct exact-source proof and inspected through `grape claims --active` / `grape_get_claims`, but it proves only excerpt existence and is not yet the primary artifact retrieval input.
- Clarified artifact behavior for first durable claims: active `repository_source_excerpt_exists` claims may be rendered in a `current-valid-claims` artifact section with claim/proof dependencies, while broader durable claim retrieval remains pending.
- Clarified the first compression cache behavior: deterministic `symbol_outline` artifacts are derived cache with stored input hashes and artifact dependencies, may render as orientation, and cannot provide proof or replace high-risk exact context.
- Clarified rule digest compression behavior: deterministic `rule_digest` artifacts are derived from verified active rule excerpt proofs and may provide orientation only; they do not replace pinned exact rule text or act as proof.
- Clarified context pack summary compression behavior: deterministic `context_pack_summary` artifacts are derived from session-scoped sent ledger rows after durable pack persistence, exclude invalidated/compression/other-branch rows, and are cached without artifact rendering until finer-grained compression invalidation is implemented.
- Clarified the session inspection surface: `grape sessions` is implemented as a read-only CLI debug command over context session and diff ledger metadata.
- Clarified the stale inspection surface: `grape stale` is implemented as a read-only CLI debug command over emitted `INVALIDATE_PREVIOUS` pack ledger rows, not as predictive stale analysis.
- Clarified the MCP stale inspection surface: `grape_get_stale_items` exposes the same emitted invalidation metadata without `rootPath` or context bodies.
- Clarified the MCP rules inspection surface: `grape_get_rules` exposes current Git-visible rule excerpts after source-hash verification and artifact secret scanning, without parsed durable `project_rules` or absolute local root paths.
- Clarified the first restricted MCP write behavior: `grape_record_command_result` and `grape_record_test_result` store agent-reported command/test observations as temporary source evidence with hashes only, require an existing current context session, reject MCP-minted observed-run authority, and do not promote durable claims.
- Clarified the remaining restricted MCP write behavior: `grape_record_candidate`, `grape_record_user_decision`, and `grape_request_user_confirmation` are implemented as non-promoting temporary evidence/request surfaces. Candidate writes link to `claim_candidates` with proof-required rejection metadata, user-decision writes store prompt/response hashes only, and confirmation requests return request IDs without persisting durable truth.
- Clarified the conflict inspection surface: `grape conflicts` and `grape_get_conflicts` inspect existing conflict-like `claim_edges` rows without claiming contradiction detection or automatic resolution.
- Clarified V1 recovery guidance behavior: status, doctor, context compile, stale restore, lock-conflict, and privacy/redaction failure paths return actionable next steps without exposing private file contents or secrets.
- Clarified partial-bootstrap config recovery: malformed or schema/project-identity-incomplete local `.grape/config.json` files are repairable by backing up the old file and writing a fresh local config, while unsupported schema versions fail closed.
- Clarified compiler source-tree ownership: `src/core/compiler/` is split into artifact, pack, repository, repository section, and repository policy subdirectories, with `src/core/compiler/index.ts` remaining the public export boundary.
- Clarified the first benchmark harness contract: `grape bench --fixture <name>` uses a named copied fixture repo, runs the real local compile/diff path twice, and fails threshold checks on unsafe omission, stale sends, missing no-change omission, missing restore hints, or low second-turn token reduction.
- Clarified context-pack Markdown output: JSON remains the canonical machine contract, while Markdown must render enough structured artifact, diff, dependency, restore, token, and safety metadata for human and agent inspection.
- Clarified bootstrap detection implementation boundaries: setup may report manifest/config-derived project hints and candidate rules, but candidate rules are not durable decisions and raw script bodies are not exposed.
- Clarified compiler repository ownership: dependency manifests, proof refs, integrity validation, rendering contracts, and selection logic live in focused compiler subdirectories under `src/core/compiler/repository/` while the public export boundary remains `src/core/compiler/index.ts`.
- Clarified repo scanner safety behavior: oversized and binary-looking files are rejected before source evidence ingestion, persisted as source rejections with metadata hashes/sizes only, and summarized through setup/status scan diagnostics.
- Clarified Git snapshot module ownership: Git orchestration remains in `repo-snapshot.ts`, while file manifest hashing, source-kind classification, and scanner rejection policy live in `file-manifest.ts`.
- Clarified cross-platform indexing path behavior: file-index path helpers normalize separators and reject traversal or drive-qualified repo paths before file reads.
- Clarified Git source-scope behavior: allowed source evidence now distinguishes committed, staged, unstaged, and untracked files using Git porcelain status.
- Clarified compiler section ownership: `sections/` assembles repository-derived artifact sections and `sections/builders/` owns individual section families.
- Clarified CLI fallback behavior: `grape sync` refreshes local snapshot/evidence/index inputs without context output, and `grape diff-context --task <text>` names the explicit compile-plus-session-diff path.
- Clarified privacy doctor behavior: `grape doctor --privacy` is a diagnostic-only command and does not approve private reads, export data, purge data, or reveal rejected-file contents.
- Clarified token-budget pruning behavior: budgeted compile protects task, pinned, exact/safety-critical, omission/restore, and invalidation context; optional non-safety context may be omitted from public pack items and output sections with explicit `omittedDueToBudget` metadata.
- Clarified the V1 artifact annotation boundary: agent-authored annotations are deferred from rendered Context Artifacts, and restricted MCP writes remain non-authoritative evidence/candidate/confirmation surfaces until proof and scope validation promote durable truth.
- Clarified naming standards: implementation filenames and source symbols should use purpose-based names instead of release-stage or version prefixes, except for external protocol names, artifact-format identifiers, and versioned documentation namespaces.
- Clarified storage migration rename behavior: migration identity is guarded by ID and checksum; filename-only drift is tolerated when content is unchanged so purpose-based renames do not break existing local bootstrap state.
- Clarified exact-source excerpt selection behavior: when task retrieval supplies query terms, exact proof windows may anchor around the first matching line in an allowed source instead of only using the beginning of the file; source hashes, excerpt hashes, and line spans remain required proof inputs.
- Clarified lexical search runtime portability: V1 search uses normal SQLite text rows and app-owned deterministic matching instead of requiring SQLite FTS5 extension support; the previous FTS5 migration checksum remains compatible for existing local databases.
- Clarified CLI runtime compatibility behavior: `node:sqlite` remains the selected storage primitive, the published package runtime floor is Node.js 22.13+, and storage-backed commands must fail before importing SQLite services on older Node versions while static setup guidance remains available.
- Clarified exact-source excerpt ranking: task-selected symbol line anchors may guide proof windows before query-term fallback, while source hashes, excerpt hashes, and line spans remain required proof inputs.
- Clarified test seed retrieval: path-like `tests` seeds may select matching allowed test source files for exact source excerpts, while non-path test names stay retrieval terms and test-source excerpts do not prove execution or correctness.
- Clarified related test retrieval: import edges from test files to task-selected source files may add related exact test source context, but import relationships remain orientation rather than proof of behavior or test execution.
- Clarified package distribution requirements: the npm package must include the compiled `grape` CLI entrypoint and runtime SQL migrations needed for local bootstrap, and package verification must fail if global-install contents drift.
- Clarified compression summary rendering: `context_pack_summary` may render as non-proof deterministic orientation only after rebuilding from current active sent-ledger inputs, and durable diff should invalidate only sent sections whose own dependency refs became stale when section-level refs are available.
- Clarified task-scoped proof and claim retrieval: when retrieval selects concrete source refs, exact-source proof creation and artifact `current-valid-claims` rendering stay scoped to those refs; broad exact-source fallback is used only when no source refs are selected, and claim inspection commands can still list all current-valid claims.
- Clarified multi-window exact source proof selection: a selected source may emit up to two non-overlapping bounded excerpts from distant task-selected symbol anchors; query-term windows are fallback behavior when no symbol anchors exist for that source.
- Clarified context pack summary freshness: prior sent ledger rows must be filtered through current artifact dependency staleness before rendering `context_pack_summary`, so a summary cannot mention rows the same compile invalidates.
- Clarified partial-bootstrap database repair: bootstrap-capable flows may back up and recreate unusable local SQLite state, while status and doctor remain diagnostic-only.
- Clarified default MCP context transport: `grape_get_context` defaults to `outputMode: "agent_pack"` with compact pack items and `artifactRef`; embedded full `contextArtifact` output, inline Markdown, and graph adjacency are available through `outputMode: "full"` or artifact inspection. MCP tool text content is a short summary rather than duplicated pretty JSON.
- Clarified storage performance hardening: local compile may reuse a caller-captured immutable repo snapshot, skip repeat evidence/index materialization only when expected rows are already present, and use append-only lookup indexes plus storage-scoped ledger queries without changing diff safety semantics.
- Clarified lexical retrieval optimization: storage may prefilter lexical candidates with bounded SQL, but the deterministic normalized matcher remains the final match authority and fallback path.
