# V1 Implementation Status

This matrix tracks implementation against the final V1 core-pipeline acceptance areas. It is an implementation aid, not a substitute for working code, tests, docs, review, and pushed commits.

Status values:

- Not started
- In progress
- Implemented
- Tested
- Documented
- Reviewed
- Done

## Alpha.3 Post-Publish Pre-Beta Status

This note captures the real pre-beta state for human review. The detailed matrix below remains the implementation-area tracker.

Done:

- `grape-context@0.1.0-alpha.2` is published and the release exists.
- `grape-context@0.1.0-alpha.3` is published on npm, tagged as both `latest` and `alpha`, and has a GitHub tag/release.
- The context transport wedge is proven through CLI and MCP `grape_get_context`.
- MCP stdio smoke exists.
- Same-session two-turn omission works when task/session identity is stable.
- `PINNED`, `OMIT_UNCHANGED`, `RESTORE_AVAILABLE`, and `INVALIDATE_PREVIOUS` are implemented in the current transport slice.
- Branch/dependency invalidation, explicit reset invalidation, and omitted restore are proven by behavior coverage.
- Packaged install smoke now asserts exact tarball selection, installed package metadata, CLI help/init/two-turn compile, CLI omitted-item restore, task/session mismatch recovery, reset recovery, MCP `initialize` / `tools/list`, two-turn context diffing, and omitted-item restore.
- Alpha e2e smoke now uses a repo-local npm cache, asserts exact tarball/package metadata, checks restore hints, and exercises installed MCP stdio setup.
- Branch-switch and stale-source fixture metadata is aligned to invalidation benchmark behavior.
- Session reset fixture benchmark proves explicit reset emits `INVALIDATE_PREVIOUS`, sends new current context, and avoids `OMIT_UNCHANGED` on the reset turn.
- Restore-path golden tests cover `RESTORE_AVAILABLE` restore IDs, session-bound lookup, restored body shape, and MCP no-root-path output.
- Dedicated task/session mismatch exit classification returns code `6` from CLI compile/diff-context instead of the storage/schema bucket.
- Package-lock metadata is aligned to alpha.3. Global install smoke passed against registry-installed alpha.3, and the external benchmark workspace dependency metadata plus published-package smoke now pass against registry-installed alpha.3.
- Storage repository ownership is split into table-family directories without schema or behavior changes.
- The external benchmark workspace has a scripted pass when run with the documented methodology and stable task/session contract.
- GitHub Actions run `27428564047` passed `npm run check` on Ubuntu, macOS, and Windows, plus the beta smoke job.

Now:

- Prove the published alpha.3 workflow across clean consumer repos with real MCP clients, not only scripted smoke.
- Keep future version bumps, tags, GitHub releases, npm publishes, and dist-tag changes approval-gated.

Next:

- Beta candidate only after install/connect/recover flows are boring across clean consumer repos.

Soon:

- Better TypeScript compiler retrieval and exact-span ranking.
- Stronger observed-run trust workflows beyond the narrow result claim.
- Stronger trust/proof depth, broader benchmark fixtures, and better dirty-worktree/session handling.

Later:

- Embeddings, Rust, cloud/team sync, full graph extraction, memory-platform features, IDE plugins, and stable `1.0.0`.

See [`beta-readiness.md`](beta-readiness.md) for the review checklist and benchmark workspace alignment note.

| Area | Status | Current evidence | Remaining work |
|---|---|---|---|
| CLI setup flow | Tested | `grape init --connect`, `grape help`, `grape sync`, `grape compile --task <text>`, `grape compile --task <text> --reset-session`, `grape diff-context --task <text>`, `grape run --session <id> -- <cmd...>`, `grape test --session <id> -- <cmd...>`, `grape sessions`, `grape stale`, `grape conflicts`, `grape artifacts`, `grape artifacts --artifact <id>`, `grape bench --fixture <name>`, `grape proofs`, `grape proofs --proof <id>`, `grape omitted --session <id>`, `grape omitted --session <id> --token <restoreToken>`, `grape status`, `grape doctor`, `grape mcp --print-config`, and `grape mcp --stdio` have thin adapters and behavior tests. Package dry-run checks verify the documented global-install package includes the compiled CLI entrypoint and runtime storage migrations. `npm run install:check` packs the exact tarball, installs it in a consumer repo, asserts installed package metadata, smoke-tests the declared installed package `bin` entrypoint through help/init/two-turn compile, CLI omitted restore, task/session mismatch recovery, reset recovery, MCP `initialize` and `tools/list`, MCP two-turn diffing, and MCP omitted restore. `npm run global:smoke` checks the registry-installed global package and exercises CLI/MCP setup, omission, restore, mismatch, and reset from a clean temp repo. `npm run e2e:alpha` uses a repo-local npm cache and exercises installed CLI/MCP setup plus the benchmark suite. | Broaden remaining CLI inspection commands after conflict creation flows exist; keep packaged install, global install, MCP smoke, and e2e coverage aligned with alpha.3. |
| Local bootstrap | Tested | `grape init --connect` creates `.grape/`, writes config, applies SQLite migrations including compression cache tables, persists a Git snapshot, persists source/source-rejection evidence rows, persists lightweight file index rows, and excludes `.grape/` through `.git/info/exclude`. It reports bootstrap detection for languages, frameworks, package manager, script names, derived commands, test command, entry points, config files, candidate rules, confidence levels, and warnings without making candidate rules durable, and reports scan diagnostics for visible/rejected file counts. Repairable malformed or schema/project-identity-incomplete local config is backed up to `.grape/config.invalid.<timestamp>.json` before a fresh config is written; unsupported future config schemas fail closed. Unusable local SQLite state is backed up to `.grape/grape.db.invalid.<timestamp>` and recreated by bootstrap-capable flows. `grape compile --task <text>` auto-bootstraps the same local state if needed, including repairable malformed config and unusable database recovery. Packaged builds copy SQL migrations into `dist/core/storage/migrations/` so global installs can apply local migrations without TypeScript source files. | Add broader recovery for future privacy approval/export/purge state once those flows exist. |
| Project scanner | Tested | Git-backed snapshot captures branch, commit, dirty paths, dirty source scopes from Git porcelain status, Git-visible files, file hashes, source-kind classification, Git-visible ignored/private path rejections, `.gitignore`, and local privacy ignore files before reading allowed file bytes. It rejects oversized and binary-looking files before source evidence ingestion, records privacy-safe rejection metadata only, and exposes aggregate scan diagnostics through init/status output. Ignored untracked paths are skipped rather than enumerated. | Add approval records and richer scan diagnostics by command/source kind. |
| File indexing | Tested | Allowed snapshot files are indexed into source-anchored module/symbol nodes and `contains`/`imports` edges with confidence and discovery method; private/ignored sources are excluded; symlinks, binary files, and snapshot-hash mismatches are skipped. The lightweight TypeScript/JavaScript signal includes function/class/interface/type declarations, constants, variables, and const-assigned arrow/function declarations. Safe portable lexical entry metadata/search rows are persisted for allowed source records after the same file guards plus secret-looking text rejection and now feed task source selection without requiring SQLite FTS5 extension support. Common-language fallback and explicit package-path scoping have checked-in behavior fixtures. | Add provider dispatch, first-class capability diagnostics, deeper package/workspace boundary policy, broader language extraction, symbol body ranges, richer blind-spot reporting, and broader current-valid retrieval over durable claims. |
| Dependency tracking | Tested | Durable context build persists artifact dependency manifests and invalidates stale sent items when section dependency refs/hashes change. Repository-derived artifacts include repo snapshot, worktree, source/config/lockfile, symbol relationship, claim, proof, and deterministic `symbol_outline`/`rule_digest`/`context_pack_summary` compression dependencies when rendered. `context_pack_summary` cache rows store sent-ledger input hashes after durable pack persistence for the next turn and are rebuilt from currently valid active sent-ledger inputs before rendering, after filtering out rows the current artifact dependency check will invalidate. Public V1 `ContextArtifact` outputs include dependency manifests with invalidation targets, and CLI/MCP artifact inspection exposes stored dependency rows. | Broaden invalidation coverage for stale durable claims and rules beyond current section dependency checks. |
| Branch awareness | Tested | Repo snapshots persist branch, head commit, dirty state, worktree hash, and sent ledgers include branch/commit identity. Explicit session reuse after a Git branch switch now updates session compile state under lock, records a `branch_changed` invalidation event, and emits `INVALIDATE_PREVIOUS` for stale branch-scoped sent context through CLI and MCP. Current-valid claim inspection excludes claims whose branch/commit scope does not match the current Git snapshot. | Add richer branch/global scope distinction and claim invalidation events beyond inspection. |
| Session diffing | Tested | Durable build proof persists session-scoped sent/omitted ledgers, safe unchanged omission, restore metadata, pinned resend, section-scoped stale dependency invalidation, stale compression-orientation invalidation, branch-switch invalidation, explicit session reset invalidation/full resend, V1-shaped `ContextPackItem` output mapping, and CLI/MCP restore lookup that verifies stored artifact/dependency/proof integrity and rejects stale dependencies before returning omitted bodies. | Add richer stale/claim invalidation once durable claims exist. |
| Task-specific context pack generation | In progress | `grape compile --task <text>` and MCP `grape_get_context` compile from real repository input, persist durable session diff rows, write public V1 context-pack JSON/Markdown artifacts, and return V1-shaped NEW/PINNED/OMIT_UNCHANGED/RESTORE_AVAILABLE context pack items across repeated session calls. CLI/MCP restore lookup can retrieve restorable omitted repository sections from internal `.repository.json` backing files when dependencies are still current. Repository-derived artifacts now include pinned active project rules, task-retrieval source hints from safe lexical/symbol/seed refs, path-like MCP test seed refs and import-related test refs as exact test source inputs, bounded exact-source evidence scoped to selected allowed sources when retrieval has concrete matches, broad exact-source fallback only when retrieval has no selected refs, multi-window symbol-anchored exact proof windows with query fallback, deterministic `symbol_outline`, `rule_digest`, and dependency-filtered prior-turn `context_pack_summary` compression orientation, a V1 `ContextArtifact`, token-budget policy that fails closed when required context cannot fit and prunes only optional non-safety sections when it can, high-risk overlay validation that fails closed unless task-selected proof-backed exact source/config/rule evidence exists, and a hardened Markdown rendering with artifact summary, diff counts, input refs, omitted/restore metadata, output sections, dependency details, token/budget status, and warnings/safety fields. | Add richer exact-span ranking across tests, richer compression replacement policy, and stronger compiler policies. |
| Proof/evidence model | Tested | Shared trust/proof/evidence shapes exist; snapshot source ingestion persists allowed source records and privacy-safe source rejections without reading rejected bytes. The repository artifact compiler now creates deterministic exact source/rule proof refs only after source bytes match stored source hashes, with up to two non-overlapping symbol-anchored windows per selected source and query-term fallback when no symbol anchors exist, and avoids creating unrelated source proof rows when retrieval already selected concrete source refs. Path-like test seed refs and import-related test refs can select test file excerpts, but those proofs only prove source existence and never test execution or behavior correctness. Local compile validates accepted exact source/rule excerpt proofs against trusted allowed source records, persists direct `proofs` rows, stores excerpt hashes as proof dependencies, creates claim candidates for `repository_source_excerpt_exists`, `repository_symbol_declaration_exists`, and parsed `project_rule`, rejects candidates without validated proofs, persists verified narrow source-excerpt/symbol/rule claims, and links proof rows to accepted claims. Parsed project-rule claims can create conservative `needs_review` conflict edges when deterministic rule text has opposing overlapping instructions, and local CLI resolution records non-conflict resolution edges without refuting or promoting claims. Current-valid filtering now blocks claims affected by explicit active `contradicts`, `violates`, and `supersedes` edges while leaving `needs_review` as inspection metadata. CLI `grape run` / `grape test` execute local commands and persist Grape-observed trusted `command_run` / `test_run` source rows with observed run IDs, command/output hashes, exit code, timestamps, branch/commit/worktree/session scope, and no raw command/stdout/stderr bodies. The local runner now validates observed-run metadata, persists direct `grape_observed_run_result` proofs, creates candidates, persists verified narrow observed-run result claims, and links proofs to claims in the same storage transaction. MCP restricted write tools persist/link temporary command/test/candidate/user-decision evidence, redact raw command/stdout/stderr/prompt/response bodies from source metadata and MCP output, return confirmation request IDs without durable writes, and do not create proof or durable claim rows. | Implement broader claim types, behavior/correctness claims from observed runs, automatic conflict resolution, broader automatic contradiction/supersession detection, and richer exact proof-span ranking across tests and AST ranges. |
| Context artifact format | Tested | Artifact guards require dependency manifests, section hashes, dependency refs, exact source refs, proof refs for exact claims, and blocked-redaction rejection. Repository-derived artifacts now include repo-state, task-retrieval, source-manifest, active-project-rules, task-scoped current-valid narrow source-excerpt/symbol/rule/observed-run claims, deterministic compression orientation including prior-turn context-pack summary when available, exact-source-evidence, file-relationship, and blind-spot sections from real repo inputs. CLI compile writes public V1 `ContextArtifact` JSON plus V1 `ContextPackItem` output under `.grape/artifacts/`, while internal `.repository.json` backing files preserve restore verification. Markdown artifacts now render the structured V1 artifact with diff-state counts, pack item input refs, omitted/restore metadata, output section details, dependency manifest details, token/budget status, warning/safety fields, and explicit budget omissions. Public artifact output removes budget-pruned section bodies from `outputSections` and records them in `omittedDueToBudget`. CLI/MCP artifact inspection exposes stored metadata plus dependency rows. Focused public-contract tests validate the CLI JSON envelope, dependency/input/section refs, context-pack item shape, and MCP Markdown parity. | Broaden durable current-valid claim/proof retrieval without weakening exact-evidence boundaries. |
| MCP integration surface or contract | Tested | `grape mcp --stdio` implements framed JSON-RPC stdio for `initialize`, `tools/list`, `tools/call`, `grape_get_context`, `grape_get_artifact`, `grape_get_claims`, `grape_get_proofs`, `grape_get_rules`, `grape_get_omitted_item`, `grape_get_stale_items`, `grape_get_conflicts`, `grape_get_status`, `grape_record_candidate`, `grape_record_command_result`, `grape_record_test_result`, `grape_record_user_decision`, and `grape_request_user_confirmation`. The frame parser rejects oversized frames and malformed `Content-Length` values. `grape_get_context` reuses the local compile service, supports `resetSession: true`, applies seed files/symbols/tests to risk detection and source retrieval, treats path-like `tests` entries and import-related test files as exact test source hints, applies `tokenBudget` with required-context protection and optional pruning, and returns compact `agent_pack` transport by default with full embedded output available only through explicit `outputMode: "full"`. `grape_get_artifact` returns stored metadata and dependency rows by default; with `outputMode: "full"`, it also returns stored public artifact JSON with full context-pack item bodies. Both modes omit absolute root paths and never return internal repository backing files. `grape_get_claims` returns current-valid narrow source-excerpt, symbol-declaration, parsed project-rule, and observed-run result claims without raw excerpts, raw command/output, or absolute root paths. `grape_get_proofs` returns persisted proof row metadata without raw excerpts or absolute root paths. `grape_get_rules` returns current Git-visible, hash-verified, secret-scanned rule excerpts without absolute root paths and compile promotes parsed rule claims separately. `grape_get_omitted_item` reuses the omitted restore app service, strips absolute root paths from MCP output, and fails closed on stale dependencies. `grape_get_stale_items` returns emitted stale invalidation metadata without absolute root paths or context bodies. `grape_get_conflicts` returns open conflict-like claim edge metadata without resolving claims or returning root paths; manual resolution is CLI-only. `grape_get_status` returns setup recovery guidance with local diagnostics. Restricted write tools record only temporary evidence, non-durable candidates, or confirmation request IDs; they require a current session and cannot create durable proofs, claims, or conflict-resolution edges. `grape mcp --print-config` now emits a usable stdio connection shape. | Harden broader durable retrieval without weakening MCP write boundaries. |
| Doctor/status/debug commands | Tested | `grape status` and `grape doctor` inspect config, database readability, migrations, Git state, Node runtime, worktree state, and local privacy exclusion. Present but unusable local databases fail database/migration diagnostics with recovery guidance instead of being treated as healthy. Storage-backed commands now check Node.js 22.13+ before importing SQLite-backed services in the published alpha package, while static help/MCP guidance remains available on older runtimes and `grape doctor --json` can emit a minimal `node_runtime` failure. `grape doctor --privacy` reports local-first defaults, `.grape/` exclusion, aggregate scanner rejections, ignored/private handling, and artifact secret-scan coverage without file bodies or secret values. `grape sessions` inspects context sessions, lock state, branch/head scope, session events, and diff ledger counts. `grape stale` inspects emitted `INVALIDATE_PREVIOUS` rows, stale reasons, prior sent item refs, and previous branch/head metadata. `grape conflicts` inspects open conflict-like claim edges, and `grape conflicts --resolve <edge> --as coexists_with|variant_of` records a manual resolution edge. `grape artifacts` inspects stored artifact metadata/dependencies, `grape claims --active` inspects current-valid narrow source-excerpt/project-rule/observed-run claims, `grape proofs` inspects persisted proof rows, and `grape omitted` lists/restores session-scoped omitted context. | Add automatic conflict resolution only if future claim semantics can prove a winning rule or claim. |
| Error handling | Tested | CLI commands return documented exit codes for usage/storage/stale/lock/unsafe states, doctor reports setup failures, and omitted restore returns stale metadata rather than stale content when branch, commit, worktree, manifest, or file dependencies changed. `grape status`, `grape doctor`, CLI/MCP compile results, lock-conflict errors, stale restore output, and privacy/redaction failures now include recovery guidance for partial bootstrap, repairable malformed config, unusable local database backup/recreation, unsupported config schema, missing Git metadata, root mismatch, dirty scope, unsafe exact-context gaps, token budget failures, lock contention, and privacy blocks. | Add recovery guidance for future privacy approval/export/purge flows and any new commands as they land. |
| Cross-platform behavior | Tested | Core paths normalize separators, Git commands use `execFileSync` args, setup uses Node filesystem APIs, and tests use real temp repos. Focused behavior tests cover Windows-style separators for privacy ignores, indexing safe-path resolution, import resolution, traversal/drive-qualified path rejection, public repo-root path labels, Windows root aliases, and repo-relative MCP artifact file refs. Runtime portability avoids SQLite FTS5 extension requirements, packages runtime SQL migrations with compiled output, and fails clearly before storage-backed CLI commands on Node versions below the published alpha package's Node 22.13 floor. GitHub Actions run `27428564047` passed `npm run check` on Ubuntu, macOS, and Windows, and then passed the beta smoke job. | Prove the v6 GitHub action bump on hosted CI, keep watching Windows runner image notices, and add WSL coverage only if real beta users need it. |
| Tests | Tested | `npm run check` includes docs, fixtures, memory, architecture, storage, typecheck, package dry-run contents, and behavior tests including CLI bootstrap, CLI status/doctor recovery guidance, CLI runtime guard behavior, CLI malformed-config repair, CLI unusable-database diagnosis and repair, CLI compile auto-repair for partial bootstrap, CLI V1 `ContextArtifact` public JSON output, CLI V1-shaped context pack item output, CLI public context artifact contract envelope, CLI unsafe compile recovery guidance, CLI lock-conflict recovery guidance, CLI proof-row persistence, CLI proof inspection, CLI claim inspection, CLI conflict inspection, CLI session inspection, CLI stale invalidation inspection, CLI task source retrieval, CLI token-budget unsafe handling, CLI fixture benchmark token-reduction thresholds, CLI active rules/exact-source evidence/compression/context-pack-summary output, CLI branch-switch invalidation, CLI session reset full resend, CLI artifact inspection, CLI omitted restore/list/stale rejection, CLI proof-dependency stale restore rejection, CLI observed command/test runner trusted source persistence and observed-run result proof/claim promotion, CLI repo-scoped privacy redaction, tampered repository-backing restore rejection, blocked redaction restore rejection with recovery guidance, claim-edge conflict repository filtering, current-valid contradiction/supersession filtering, MCP stdio `grape_get_context`, MCP compact default output, MCP full lazy artifact fetch, MCP V1 `ContextArtifact` output, MCP V1-shaped context pack item output, MCP Markdown/context-pack item parity, MCP seed-file/test-seed/related-test retrieval, MCP token-budget evaluation and recovery guidance, MCP branch-switch invalidation, MCP session reset full resend, MCP `grape_get_artifact`, MCP `grape_get_claims`, MCP `grape_get_proofs`, MCP `grape_get_rules`, MCP `grape_get_conflicts`, MCP status recovery guidance, all restricted MCP write tools, MCP write-boundary rejection for agent-minted observed-run authority, unsupported candidate promotion, and bad user-decision hashes, MCP `grape_get_omitted_item`, MCP `grape_get_stale_items`, MCP stale restore rejection, MCP session isolation, MCP `--repo` launch, MCP frame-size and malformed `Content-Length` rejection, section-scoped dependency staleness, artifact secret scan, evidence source persistence, exact source proof persistence/rejection, observed-run result proof/claim promotion and raw-metadata rejection, multi-window exact source excerpt anchoring, context-pack-summary stale-row exclusion, source-excerpt claim promotion/rejection, file indexing including const-assigned function symbol detection, safe lexical persistence/search, deterministic `symbol_outline`/`rule_digest`/`context_pack_summary` compression cache storage, common-language fallback fixtures, explicit package-path scoping fixtures, task source retrieval, rule-file classification, repository-derived artifact compilation with proof refs, and output-preparation rollback. | Add artifact schema golden tests for broader durable claim/proof retrieval. |
| Documentation | Documented | CLI, README, roadmap/log/changelog, architecture, ADRs, storage, security, trust, retrieval, artifact contract, diff contract, MCP contract, beta readiness, beta trial checklist, and this status matrix are updated for the setup/evidence/indexing/compiler/CLI-compile/MCP-get-context/omitted-restore/artifact-inspection/compression-staleness slices. The root README now links to contributing guidance instead of agent-only rules, and the root roadmap uses product-focused now/next/later buckets. Compiler internals now have documented ownership subdirectories under `src/core/compiler/`. | Keep docs aligned as each core-pipeline slice lands. |
| Known limitations | Documented | Current docs state CLI setup, alpha.3 hardening-candidate readiness, package dry-run readiness for the documented global-install contents, snapshot source persistence, safe portable lexical persistence/search rows, deterministic TypeScript/JavaScript AST graph indexing for common symbols/imports/exports/calls, task source retrieval, path-like MCP test seed retrieval, import-related test retrieval, Grape-observed command/test source evidence plus narrow observed-run result proof/claim promotion, token-budget required-context protection and optional pruning, deterministic `symbol_outline`, `rule_digest`, and stale-filtered prior-turn `context_pack_summary` compression cache orientation, repository-derived artifact compilation with pinned active project rules, multi-window symbol-anchored bounded exact source evidence with query fallback, task-scoped exact proof/claim artifact rendering, persisted exact source proof rows, first high-risk exact-context gating, narrow durable source-excerpt/symbol/rule claim promotion, conservative project-rule conflict creation and manual CLI resolution, current-valid claim artifact sections, CLI/MCP claim/proof/conflict inspection, MCP current rule excerpt inspection, MCP temporary restricted write tools, multi-fixture benchmark shell and external scripted benchmark pass, public V1 `ContextArtifact`, hardened Markdown context-pack rendering, CLI/MCP session diffing, branch-switch/session-reset/section-stale invalidation, CLI/MCP stale invalidation inspection, artifact inspection, and omitted restore lookup exist. Current docs also distinguish safe polyglot fallback from proven language-aware graph extraction. Broader durable claim types, behavior/correctness/root-cause claims from observed command/test runs, predictive stale analysis, automatic conflict resolution, semantic/embedding retrieval, broader benchmark gold labels, provider dispatch, deeper package/workspace invalidation, benchmark baselines for polyglot and monorepo scenarios, richer AST/test exact-span ranking, richer compression replacement policy, rendered agent-authored artifact annotations, and broad language parsing remain pending. | Revisit after each slice; do not mark Done until limitations are narrowed to acceptable V1. |
| Final human-review readiness | Documented | Minimum doc/onboarding bar for a serious pre-beta technical review: honest roadmap/status, CLI/MCP command-status tables, artifact authority warnings, the agent session contract, beta trial checklist, and examples derived from tests. `npm run check` passes on a clean clone (CI Node 22.13+). ADR-0010 and transport-first roadmap are published. `grape-context@0.1.0-alpha.3` is published with exact packaged install smoke, global install smoke, npm bin entry, MCP stdio smoke, Grape-observed command/test source evidence and narrow observed-run result proof/claim promotion, two-turn omission, invalidation, restore coverage, session-reset benchmark coverage, restore-path goldens, dedicated task/session mismatch exit classification, package-lock metadata alignment, storage repository ownership split, benchmark/e2e gates, and external alpha.3 benchmark workspace smoke. | **Ready** for serious pre-beta human review of docs/contracts/current transport slice. **Next before beta:** prove install/connect/recover flows across clean consumer repos with real MCP clients using `beta-trial-checklist.md`. **Not ready** for full product sign-off or finished broad durable-truth retrieval. |
